[project]
name = "healthcare-domain-assistant"
version = "0.1.0"
description = "Healthcare FAQ assistant fine-tuning project"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
training = [
  "torch",
  "transformers",
  "datasets",
  "accelerate",
  "peft",
  "trl",
  "pandas",
  "notebook",
  "unsloth",
]
